services:
  frontend:
    build:
      context: ./
      dockerfile: docker/Dockerfile
    container_name: starsoft-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production